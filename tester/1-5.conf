server {
		listen 8080;

		server_name localhost;
		root ./tester/www;
		method GET;
}
server {
		listen 8081;

		server_name localhost;
		root ./tester/www;
		method POST;
}
server {
		listen 8082;

		server_name localhost;
		root ./tester/www;

		
		method HEAD;
}
